<% @posts.each do |post| %>
    <div class="card p-2">
        <h4><%= link_to post.title, publication_post_path(post.publication_id, post) %></h4>
        <p><%= truncate post.body, length: 200 %></p>
    </div>
    <br>
<% end %>